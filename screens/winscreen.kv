#:kivy 2.0.0
#:import BackGround2 backgrounds.background2
#:import FrameBase figures.framebase
#:import HangManFig3 figures.hangmanfig3

<WinScreen>:
    name: '_win_screen_'
    BackGround2:
 
    CustomButton:
        text: 'Back'
        icon: "skip-previous"
        pos_hint: {'x': 0, 'y': 0}
        on_release:
            root.win_popup()

    RelativeLayout:
        canvas.before:
            Color: 
                rgb: 0, 0, 0

        id: animation_area
        size_hint_x: 0.7
        HangManFig3:
